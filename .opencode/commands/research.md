---
description: Run a named repository research prompt and write documentation only
agent: build
---

Load the `research` skill and execute it for this prompt name:

`$ARGUMENTS`

The prompt name must resolve to `context/research/<prompt-name>.md`. Follow the skill's documentation-only constraints and write the result to the output declared by the prompt.
