# Plan: Update Seed Data with Icon Library Names

## Summary

Update `prisma/seed.ts` to use Material Symbols icon names (instead of emojis) for the `ItemType.icon` column, add the full seed data from `seed-spec.md`, and update `context/current-feature.md`.

## Key Findings

- The `icon` column is `TEXT` — no migration needed, just update values via seed upsert
- Project uses **Google Material Symbols** (CSS-loaded), not Lucide React
- Frontend `getItemTypeIcon()` in `MainContent.tsx` hardcodes icon names — should be updated to read from DB
- The seed-spec mentions "Lucide React or whatever icons are available" — Material Symbols is what's available

## Material Symbols Icon Mapping

| ItemType | seed-spec icon | Material Symbols name |
|----------|---------------|----------------------|
| snippet | Code | `code` |
| prompt | Sparkles | `auto_awesome` |
| command | Terminal | `terminal` |
| note | StickyNote | `sticky_note_2` |
| file | File | `description` |
| image | Image | `image` |
| link | Link | `link` |

## Changes

### 1. Update `context/current-feature.md`
- Add new feature section for seed data implementation
- Reference `seed-spec.md`

### 2. Update `prisma/seed.ts`
- Replace emoji icons with Material Symbols names
- Add user seed (demo@devvault.io)
- Add collections and items per seed-spec:
  - React Patterns (3 snippets)
  - AI Workflows (3 prompts)
  - DevOps (1 snippet, 1 command, 2 links)
  - Terminal Commands (4 commands)
  - Design Resources (4 links)

### 3. Update `src/components/main/MainContent.tsx`
- Modify `getItemTypeIcon()` to accept icon name from DB instead of hardcoding
- Or keep the hardcoded map but ensure it matches the new icon names

## Files to Modify

- `C:\test\devstash\context\current-feature.md`
- `C:\test\devstash\prisma\seed.ts`
- `C:\test\devstash\src\components\main\MainContent.tsx` (optional — align hardcoded map)

## Verification

1. Run `npx prisma db push` (no schema change, but ensure DB is in sync)
2. Run `npx prisma db seed` to apply new seed data
3. Query `item_types` table to verify icon column has Material Symbols names
4. Run `npm run build` to verify no type errors
5. Run `npm run dev` and check sidebar renders correct icons
