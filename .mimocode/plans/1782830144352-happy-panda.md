# Plan: Add Profile Page Feature to current-feature.md

## Objective
Update `context/current-feature.md` to add the Profile Page feature as Phase 17, setting status to "In Progress" and adding goals and history from the profile-spec.md file.

## Changes to Make

### 1. Update Status (Line 4)
Change from:
```markdown
<!-- Not Started|In Progress|Completed -->
```
To:
```markdown
In Progress
```

### 2. Add Goals (After Line 7)
Add the following goals based on profile-spec.md:
```markdown
- Create profile page at `/profile` route
- Display user info: email, name, avatar (GitHub or initials), account creation date
- Show usage stats: total items, total collections, breakdown by item type
- Add account actions: change password (email users only), delete account with confirmation
- Follow existing codebase patterns for data fetching and components
```

### 3. Add Reference to profile-spec.md (After Line 16)
Add:
```markdown
- @context/features/profile-spec.md
```

### 4. Add History Entry (After Line 69)
Add:
```markdown
- **Phase 17 Started**: Profile page — user info display, usage stats, change password (email users only), delete account with confirmation, protected route.
```

## Files to Modify
- `context/current-feature.md`

## Verification
- Check that all comments are preserved
- Verify status is set to "In Progress"
- Verify goals are added correctly
- Verify history entry is added at the end
- Verify profile-spec.md reference is added