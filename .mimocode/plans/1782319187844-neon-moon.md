# Plan: Update current-feature.md with Stats & Sidebar Feature

## Objective
Add the stats-sidebar-spec as the new current feature in `context/current-feature.md`, set status to "In Progress", and keep all existing HTML comments.

## Current State of `context/current-feature.md`
- Status: Completed (Phase 7)
- Goals: empty
- History: Phases 1-7 all completed

## What the New Feature Requires (from stats-sidebar-spec.md)

| Requirement | Current State |
|---|---|
| Stats in main area from DB data | **Already done** (Phase 7) |
| Item types in sidebar with icons, linking to /items/[typename] | **Not done** — sidebar has hardcoded `MENU_ITEMS` nav |
| "View all collections" link under collections list | **Not done** |
| Sidebar favorites from DB (keep star icons) | **Not done** — uses `mock-data.ts` |
| Sidebar recent collections from DB (colored circle by most-used item type) | **Not done** — uses `mock-data.ts` |

## Action

Edit `context/current-feature.md`:

1. **Line 6**: Change `Completed` → `In Progress`
2. **Line 9-10**: Add goals from the spec (keep comment placeholder)
3. **Add reference**: `@context/features/stats-sidebar-spec.md` to the references list
4. **Keep all HTML comments** (`<!-- ... -->`) intact

## Verification
Run `npm run dev` and check the sidebar renders real DB data instead of mock data.