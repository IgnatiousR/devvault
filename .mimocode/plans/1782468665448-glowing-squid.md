# Plan: Update current-feature.md for Auth Phase 1

## Summary
Update `@context/current-feature.md` to reflect the new authentication feature based on the auth-phase-1-spec.md specification.

## Changes to `current-feature.md`

### 1. Title
- Change from: `# Current Feature: Client-Side Data Fetching with Skeleton Loading`
- Change to: `# Current Feature: Auth Setup - Better Auth + GitHub Provider (Prisma v7)`

### 2. Status
- Set to: `In Progress`

### 3. Goals
Replace existing goals with auth-phase-1 requirements:
1. Install Better Auth and Prisma adapter packages
2. Configure Better Auth with GitHub OAuth provider
3. Set up Prisma v7 client singleton and schema
4. Create auth API route handler
5. Create client-side auth utilities
6. Protect `/dashboard/*` routes using Next.js 16 Proxy
7. Redirect unauthenticated users to sign-in page
8. Implement server-side session validation

### 4. References
- Add: `@context/features/auth-phase-1-spec.md`
- Keep existing Prisma docs references (still relevant)

### 5. Notes
- Update with Better Auth specifics:
  - Uses Prisma v7 with custom output path
  - Better Auth session management (not JWT)
  - GitHub OAuth via environment variables
  - Named export for proxy (not default)

### 6. History
Add new entry at the end:
```
- **Phase 11 Started**: Auth setup with Better Auth + GitHub Provider. Configuring Prisma v7 adapter, GitHub OAuth, route protection via Next.js 16 Proxy, and session management.
```

## Verification
After updating, verify the file reflects:
- Correct feature title matching the spec
- Status shows "In Progress"
- All goals from the spec are captured
- History entry added chronologically
- References include the auth spec file